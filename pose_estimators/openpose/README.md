# OpenPose

Additional arguments specific to Openpose that can be passed to the estimator via `--args` when the main script is
called:

`--chunks` to specify the number of chunks to split the input videos into, and the number of resulting jobs to submit.